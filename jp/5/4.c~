#include <stdio.h>

int main()
{
    int a, suma=0, i = 1;

    printf("Podaj górną granicę: ");
    scanf("%d", &a);

    while (i < a) {
      suma+=i;
	++i;
    }
	printf("%d\n",++a);
    return 0;
}
